// HTTP API Example - demonstrates AnsClient with VerificationPolicy

application {
    mainClass.set("com.godaddy.ans.examples.httpapi.HttpApiExample")
}

dependencies {
    // Uses only the SDK - no additional dependencies needed
}